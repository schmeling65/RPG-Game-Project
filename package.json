{
  "name": "rpg-project",
  "version": "0.0.1",
  "description": "my own rpg project",
  "main": "index.js",
  "scripts": {
    "run": "neu run",
    "build": "neu build --release",
    "precompletebuild":"npm run build --prefix core",
    "completebuild": "neu build --clean",
    "preinstallDependencies":"npm install",
    "installDependencies": "npm -C core install"
  },
  "author": "schmeling65",
  "license": "ISC",
  "dependencies": {
    "@neutralinojs/neu": "^11.7.2"
  }
}
